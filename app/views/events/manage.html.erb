<div class="navbar-fixed">
  <%= render "layouts/admin_nav" %>
</div>
<main>
<div class="container margin-down">
  <h4 class="center">Manage Event: <%= @event.title %></h4>
    <div class="row">
      <div class="col s12">
        <ul class="tabs">
          <li class="tab col s3 "><a href="#edit-event">Edit Event</a></li>
          <li class="tab col s3"><a class="active" href="#staff">Staff</a></li>
          <li class="tab col s3"><a href="#sponsors">Sponsors</a></li>
          <li class="tab col s3"><a href="#tasks">Tasks</a></li>
        </ul>
      </div>

      <div id="edit-event" class="col s12">
        <%= render "form" %>
      </div>

      <div id="staff" class="col s12">
        <h5>Event Staff</h5>
        <hr>
        <div class="event_staffs" id="event_staffs">
          <% if @event.get_event_staffs == ""  %>
            <span>No staff has been added to this team</span>
            <p>Add implementation to delete team members</p>
          <% else %>
            <%= @event.get_event_staffs %>
          <% end %>
        </div>
        <br>
        <h5>Add Staff</h5>
        <hr>
        <div class="row">
          <%= render "staff_fields" %>
        </div>
      </div>

      <div id="sponsors" class="col s12">
        <% # = render "sponsors/event_sponsors" %>
        Figure out how to render 
        <ul>
          <li> - "sponsors/event_sponsors" </li>
          <li> - "sponsors/add_sponsors"</li>
        </ul>
         with :event_id included
        <br>
        <% #  = render "sponsors/add_sponsors" %>
      </div>

      <div id="tasks" class="col s12">
        To be implemented
      </div>
    </div>    
</div>
</main>
