<div id="att2">
  <%= attend_event_or_login( "Attend this event", nil, @var1, @var2) unless user_is_attending_event %>

  <%= link_to "Unattend".html_safe,  unattend_path(:event_id => @event.id),
                   method: :get, class: "btn waves-effect waves-light btn-large our-btn-green red darken-4", data: {id: "attend"},id: "attend", remote: :true  if user_is_attending_event%>
</div>
  <%# css_class_unattend = 'hideam' if user_is_attending_event %>
  <%# css_class_attend = 'hideam' unless user_is_attending_event %>
  <%#
      @var1 = ""
      @var2 = ""
  %>
  <%# @var1 = "#{@event.event_template} darken-4"  if controller_name == "events" && action_name == "show"%>
  <%# @var2 = "landing2" if @current_page && @current_page == "new"%>
  <%# css_class_attend =  @event.attending?(current_user)? 'hideam': '' %>
  <%#= link_to "Attend this event".html_safe, '#purchase_ticket_modal',
                class: "#{css_class_attend} modal-trigger btn waves-effect waves-light btn-large our-btn-green #{@var1} #{@var2}", data: {id: 'unattend', target: 'purchase_ticket_modal'},id:'attend'  %>
