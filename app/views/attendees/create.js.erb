$("a[data-remote]").on("ajax:success", function(e, data, status, xhr){
  $(this).hide();
  console.log("<%= require 'pry'; binding.pry %>") 
  // addClass('hideam')
  $('#'+$(this).data('id')).show();
  // removeClass('hideam')
})
